# 2026-08-24 工单同步自动化行为调整

- 统一提取增加业务输入指纹和提示词指纹缓存。相同内容重复同步时复用成功结果，评论、同步时间和记录链接变化不会单独触发提取。
- 已有标题不再作为统一字段提取和 AI 分类的总跳过条件；标题总结、字段提取、翻译和分类按各自条件独立执行。
- deferred 同步主入库阶段只保存快速同步数据，统一提取和自动化在后台后处理执行，避免主阶段与后处理重复调用 AI。
- 自动拉日志统一使用规范化运行参数，历史 `log_pull_hints.modifyTime` 可以补齐自动拉日志日期；`sourceStoreCode` 与日志接口 `storeId` 分开保存。
- POS/SCO 仍通过可配置提示词识别，代码只拦截金额误识别和无效值覆盖，`#2 POS, $44,510.00` 会保持 POS=2。
