# 工单 AI 分析结果回写修复

## 修复内容

- 修复 AI Agent 已成功返回结果，但工单任务仍长期显示“执行中”的问题。
- 当 AI 的“建议负责人”文本超过快照字段长度时，快照展示字段会截断保存，完整内容仍保留在 AI 分析结果详情中。
- 持久化阶段发生数据库异常时，服务端会先回滚当前事务，再将任务更新为“失败”并保留错误原因，避免前端持续轮询“执行中”。

## 使用影响

重新发起工单 AI 分析后，成功结果会正常出现在工单详情的“协同/AI”区域，包括 AI 消息、RCA 和 ACR 快照。历史上已卡在“执行中”的任务不会自动补写，需要在任务区域重新发起分析。
