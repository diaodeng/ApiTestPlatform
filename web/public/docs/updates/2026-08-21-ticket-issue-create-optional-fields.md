# 2026-08-21 - 问题实例新增可选字段校验修复

## 修复

- 修复新增问题实例时未选择负责人，前端提交 `ownerId: ""` 导致后端返回“参数或数据异常: ownerId”的问题。
- 后端统一将问题实例的可选整数输入（项目、模块、负责人、首张工单和影响工单数）中的空字符串归一化为 `None`。
- 前端提交问题实例时过滤空的可选字段，避免同类表单值触发整数校验错误。

## 使用说明

负责人、项目和模块可按业务需要填写；负责人不是问题实例新增的必填项。

## 影响范围

- 问题实例新增表单
- 问题实例请求模型校验
