# 修复统一凭证到期时间为空字符串提交报错

- 新增/编辑统一凭证时，若「到期时间」留空，前端会以空字符串 `""` 提交 `expireTime`，后端 `datetime | None` 解析失败，报 `datetime_from_date_parsing`。
- 修复：统一凭证编辑弹窗保存时把空字符串的 `expireTime` 归一为 `null`，后端按「未设置到期时间」处理，不再报错。
- 不使用到期时间的凭证（如手工录入的静态 API Key）留空保存即可。