# 2026-08-25 门店来源与 AI 提取值规则调整

- 明确 `sourceStoreCode` 只表示外部同步原始门店编码，AI 提取不会反写或覆盖。
- 统一 `storeId` 的选择规则：AI 值与来源编码相等或被来源编码包含时采用 AI；AI 为空或不匹配时回退来源编码；没有来源编码时有效 AI 新值可以替换旧值。
- 主同步、延后后处理和自动日志使用同一门店选择结果，并在审计中记录选择原因。
- `sourceHash` 只比较原始业务输入和提示词配置，不因 AI 结果、缓存状态或自动化审计变化而重复调用 Provider。
