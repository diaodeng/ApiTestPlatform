---
title: 文档结构与用户说明规则更新
---

## 2026-08-03

1. 帮助文档从“单纯变更记录混排”调整为“用户说明 + 更新记录”两层，用户查看文档时会优先看到功能和模块说明，更新记录单独放入 `updates/` 目录。
2. 用户说明文档要求补充功能用途、入口位置、配置项、参数示例、默认值、可选值、影响范围、注意事项和常见问题，不能只写变更摘要。
3. 新增或修改业务逻辑时，如果没有对应的用户说明文档，必须先补齐文档再交付。
4. 更新记录文件统一放入 `web/public/docs/updates/`，帮助中心会按时间倒序展示该目录中的内容。
