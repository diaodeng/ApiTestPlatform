# AGENTS.md - api-test-platform 项目提示词

## 项目结构
- `server/`: FastAPI 后端、调度与 Agent 相关服务。
- `web/`: Vue3 + Vite 前端。
- `client/`: Flet 客户端（旧版）。
- `client_new/`: PySide6 客户端（新版）。
- `web/public/docs` 说明文档目录。

## 工作原则
- 默认使用中文，先结论后细节。
- 仅做与当前需求相关的最小改动，不做顺手重构。
- 优先修复根因，避免用大范围兜底逻辑掩盖问题。
- 未经明确授权，不执行高风险操作（如批量删除、强制重置）。
- 有逻辑改动或者新增，需要新增或者更新设计和说明文档，共后续AI分析，比如这次会话修改了内容，下一次新会话能知道完整的当前项目状态。
- 项目结构、代码输出按工程化处理，不要将所有方法放在一个文件，或者各种文件都在一个目录（包）。
- 新增或改造功能必须先按作用域归类：controller 只做协议和鉴权，service 只做单一业务编排，dao 只做数据访问，util 只做无副作用纯工具；跨链路共享能力必须下沉到独立子服务或 util，不允许用主服务门面、函数内导入、延迟代理规避依赖方向。
- 单个后端 service 文件原则上不继续膨胀：新增能力优先创建按职责命名的子服务文件，例如 `*_config_service.py`、`*_notification_job_service.py`、`*_pull_service.py`、`*_comment_service.py`；只有强相关且生命周期一致的少量方法才放在同一文件。
- 禁止新增只做 re-export 或简单转发的兼容 shim；拆分时必须同步更新调用方和测试引用，确认无旧入口残留后再交付。
- 拆分后的子服务若方法会被其他文件调用，方法名必须使用公开命名，不允许以 `_` 开头；`_xxx` 仅限类内部私有辅助方法，外部调用私有方法视为需要继续下沉或改名。
- 对已有大文件继续改造时，优先迁移低风险清晰边界，再改调用方，最后删除旧方法；每次迁移都要对照历史分支或现有测试保留业务语义。
- 将每次的改动记录下来，记录到`web/public/docs`中，供后面复盘。
- 方法中都需要写清楚方法注释、实现过程都都尽量注释清楚、注释用中文。
- 实现过程中注意中文不能乱码。
- 每一次都需要有关键日子，从日志可以看出执行了什么，为什么不执行什么。
- 日志格式化不要使用%s的方式，统一使用f方式，loguru不支持%s。
- fastapi接口不要在异步接口中使用同步方法导致阻塞。
- 对外 HTTP 接口的路径、查询、请求和响应契约必须使用 Pydantic 模型定义和校验；禁止以裸 `dict` 作为接口契约。
- 业务核心数据在 service/领域层必须使用 ORM 实体或按职责定义的 `dataclass`，不得把 DAO 查询结果提前转换为 `dict` 再参与业务编排；`dict` 仅允许用于 JSON 响应序列化边界或无固定结构的扩展字段。
- 对外响应中的数据库 `BIGINT` 主键或关联 ID 若可能超过 JavaScript 安全整数范围，必须序列化为字符串；前端不得对这类 ID 使用 `Number()`，后端 Pydantic 请求模型再负责解析并校验整数。

## 文档规则
- `web/public/docs` 面向最终用户，默认只放用户可见的使用说明、配置说明、注意事项和少量更新记录入口，不把日常开发过程、实现细节和内部讨论直接写进去。
- 用户使用说明必须尽量完整、详细，至少说明清楚：有什么功能、功能是干什么的、入口在哪里、怎么使用、有哪些配置项、每个参数的含义、默认值、可选值、不同取值带来的影响、示例、注意事项和常见问题。
- 用户说明文档按功能和模块组织，新模块或新功能在有必要时可以新增独立文档；已有功能发生变化时，优先更新对应说明文档，保持与当前行为一致。
- 修改对应业务逻辑时，必须先检查是否存在对应的用户说明文档；如果没有，则必须补齐后再交付，不能只改代码不补文档。
- 每次有逻辑变更、新增功能、调整配置语义或变更使用方式时，除更新 `wiki` 外，还要同步维护 `web/public/docs` 中对应的用户说明。
- 更新记录可以保留在 `web/public/docs`，但必须放在独立目录中单独展示，不与用户说明混放；更新记录按时间倒序排列，便于用户查看最新变化。
- `web/public/docs` 的展示必须保持前端自动发现能力，禁止把文档入口改成纯手工维护菜单的方式。
- 文档命名和结构要服务于用户查找：用户说明优先体现功能和模块，更新记录优先体现日期和变更主题。

## 工程实现边界
- 后端控制器：只负责路由、参数模型、权限、异步接口中的 `run_in_threadpool` 包装和响应转换；不得堆业务规则。
- 后端服务：一个服务文件只承接一个清晰业务能力。同步、通知、评论、配置、拉取、AI、日志等能力分文件处理；跨模块编排可以调用下层服务，但下层服务不得反向依赖上层主服务。
- DAO：只封装查询和持久化，不处理业务状态机、外部 API、通知发送或 AI 逻辑。
- Util：只放纯函数、字段归一化、序列化、哈希、时间解析等无数据库和无外部网络副作用的能力。
- 定时任务：只解析任务参数、检查停止标记、创建数据库会话和调用对应服务；不得直接实现大段业务逻辑。
- 前端页面：页面负责状态和交互编排，通用表单、配置块、弹窗、选择器和展示块要拆到 `components` 或同域子组件；避免单个 `.vue` 同时承载大量无关功能。
- 配置类页面必须按主开关/Provider/模式做联动显示：只展示当前模式需要维护的配置项，不要把所有可选配置傻瓜式全量平铺；被隐藏的字段不得主动清空，切回对应模式时必须能恢复显示原值。手动执行区的 Provider/模式选项也要跟随当前配置收敛，避免用户选择当前链路不支持的组合。
- 文档和 wiki：新增结构性能力、拆分边界、规则变更必须同步更新 `wiki`，并按本文件“文档规则”同步维护 `web/public/docs` 中对应的用户说明或更新记录。

## 目录边界
- 后端接口、任务调度、服务逻辑优先改 `server/`。
- 前端页面与组件优先改 `web/src/`。
- Flet 客户端逻辑优先改 `client/src/`。
- PySide 客户端逻辑优先改 `client_new/`。
- `web/dist/` 视为构建产物，除非明确要求发布构建，不直接手工修改。


## 常用命令
- 前端开发：
- `cd web`
- `npm run dev`
- `npm run build:prod`
- 后端开发：
- `cd server`
- `uv sync`
- `uv run python app.py --env=dev`
- `uv run ruff check .`
- 旧客户端：
- `cd client`
- `uv run flet run`
- 新客户端：
- `cd client_new`
- `uv sync`
- `uv run python main.py`

## 验证要求
- 修改后至少执行与改动直接相关的检查或运行步骤。
- 若无法在本地完成验证，必须说明：
- 已执行的验证；
- 未执行项及原因；
- 剩余风险。

## 交付格式
- 最终输出按以下顺序：
- 结论；
- 变更文件；
- 变更原因；
- 验证结果；
- 剩余风险或下一步建议。

## 知识库
- 工作目录中有知识库，做任何事情前优先从知识库获取信息。
- 位置`wiki`。
